#include <iostream>
#include <cstring>
#include <string>
#include <queue>
using namespace std;
int n, m;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    while(cin >> n >> m){
    	
    }
    return 0;
}
